"use client";

import { Container } from "@mui/joy";
import FromForgetPassword from "../../components/forget-password/FromForgetPassword";

const ForgetPassword = () => {
  return (
    <Container maxWidth="xs">
      <FromForgetPassword />
    </Container>
  );
};

export default ForgetPassword;
